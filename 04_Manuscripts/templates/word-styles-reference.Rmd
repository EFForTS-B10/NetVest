---
title: "Test Report"
author: "Julia Henzler"
date: "5/23/2021"
output:
  word_document:
    reference_docx: "../templates/word-styles-reference01.docx"
bibliography: "IntegrationPaper.bib"
---
# Heading 1
My Report starts here

## Heading 2

# Make a table
Create a subset of the mtcars data. 

```{r test table,echo=TRUE}
# extract a subset of the mtcars data
library(dplyr)
df <- mtcars %>%
    mutate(make_model = row.names(mtcars)) %>%
    filter(cyl == 4) %>%    
    select(make_model, mpg, wt) %>%
    mutate(wt = wt*1000) %>%
    arrange(make_model) 
    
#Then use *knitr::kable()* to create the table.
library(pander)
panderOptions('table.alignment.default', function(df)
    ifelse(sapply(df, is.numeric), 'right', 'left'))
panderOptions('table.split.table', Inf)
panderOptions('big.mark', ",")
panderOptions('keep.trailing.zeros', TRUE)
pander(df)
```

# Make a figure

```{r test figure, out.width = "100%", fig.cap = "**InVEST-Terrestrial Biodiversity.** Calculation of grid cell-level degradation score and grid cell-level habitat quality score"}
knitr::include_graphics(file.path("InVEST.png"))
```

# References